<div class="mt-1e">
  <div class="card mb-3">
    <div class="card-header text-white bg-purple">Staff/<%= @staff.name %> 編集</div>
    <div class="card-body">
      <%= form_with(model: [:submanager, @staff], url: submanager_staff_path(current_submanager, @staff), method: :patch) do |f| %>

        <div class="staff_update_error"></div>

        <div class="form-group">
          <%= f.label :name, "名前" %>
          <%= f.text_field :name, required: true, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.label :email %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", required: true, class: "form-control" %>
        </div>

        <%= f.submit "編集", class: "btn btn-dark" %>
      <% end %>
   </div>
  </div>
</div>
