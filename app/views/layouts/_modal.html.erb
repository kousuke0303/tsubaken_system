<div id="modal-container" class="modal fade" role="dialog"></div>

<% if admin_signed_in? || manager_signed_in? %>

  <!--勤怠new・edit-->
  <% if controller_name == "attendances" && action_name == "individual" || controller_name == "attendances" && action_name == "daily" %>
    <div id="attendance-new" class="modal fade" role="dialog"></div>
    <div id="attendance-edit" class="modal fade" role="dialog"></div>
  <% end %>

  <!--matter/task担当者変更-->
  <% if controller_name == "staffs" && action_name == "retirement_process" %>
    <div id="change-member-for-matter" class="modal fade" role="dialog"></div>
    <div id="change-member-for-estimate_matter" class="modal fade" role="dialog"></div>
    <div id="change-member-for-schedule" class="modal fade" role="dialog"></div>
  <% end %>

  <!--お問合せedit-->
  <% if controller_name == "inquiries" && action_name == "show" %>
    <div id="inquiry-edit" class="modal fade" role="dialog"></div>
  <% end %>
  
<% end %>

<!--アドミン・マネージャー・スタッフ用-->
<% if admin_signed_in? || manager_signed_in? || staff_signed_in? %>

  <!--着工案件関連-->
  <% if controller_name == "matters" && action_name == "show" %>
    <!--ここまで確認済み-->
    <!--請求書関連-->
    <div id="invoice-edit" class="modal fade" role="dialog"></div>
    <div id="invoice-detail-edit" class="modal fade" role="dialog"></div>
    <div id="invoice-object-edit" class="modal fade" role="dialog"></div>
  <% end %>

  <% if controller_name == "schedules" && action_name == "application" %>
    <div id="schedule-application" class="modal fade" role="dialog"></div>
  <% end %>
  
<% end %>


<!--アドミン・マネージャー・スタッフ・外部スタッフ用-->
<% if admin_signed_in? || manager_signed_in? || staff_signed_in? || external_staff_signed_in? %>
  <!--SCHEDULEnew・edit-->
  <% if controller_name == "schedules" && action_name == "index" %>
    <div id="sales-status-edit" class="modal fade" role="dialog"></div>
  <% end %>
<% end %>



<!--------------------------------------------------------->

<!--共通-->

<% if controller_name == "static_pages" && action_name == "top" %>
  <div id="inquiry-new" class="modal fade" role="dialog"></div>
<% end %>

<% if vendor_manager_signed_in? %>
  <% if controller_name == "construction_schedules" && action_name == "index" %>
    <div id="schedule-detail" class="modal fade" role="dialog"></div>
  <% end %>
<% end %>
