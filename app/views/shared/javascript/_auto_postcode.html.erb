<script>
  $("#address_city").change(function() {
    if(!$("#postcode").val()){
      var address = $(this).val();
      $.ajax({
        type: "GET",
        url: "<%= postcode_search_path %>",
        cache: false,
        data: { address_city: address, remote: true }
      })  
    }
  });
</script>
