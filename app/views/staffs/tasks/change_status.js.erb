$('#alert_task_list').html('<%= escape_javascript(render 'shared/top_pages/alert_list/alert_task_list') %>');
$('#task_notification_lists').html('<%= escape_javascript(render 'common_layouts/notifications/task_notification_lists') %>');
$('.tasks-index').html('<%= escape_javascript(render 'staffs/tasks/partial/tasks_index') %>');
removeLoading();
toastr.success('<%= @task.title%>を<%= @task.status_i18n %>に設定しました');
<!--バッジ変更-->
<% if @reciever_notification_count %>
  var totalCount = <%= Notification.all.count %>;
  var reciever = <%= @task.member_code_id %>
  var recieverCount = <%= @reciever_notification_count %>;
  var countArrey = [totalCount, reciever, recieverCount]
  localStorage.setItem('totalNotificationCount', totalCount);
  App.badge.speak(countArrey);
<% end %>