<div class="modal-dialog no-padding_modal">
  <div class="modal-content">
    <div class="modal-header modal-header-relevant">
      <%= @staff.name %> アカウント削除</span>
      <button type="button" class="close" data-dismiss="modal">&times;</button>
    </div>
    <div class="modal-body">
      <div id="staff-destroy-errors"></div>
      <div>
        <span><i class="fas fa-exclamation-circle fa-red"></i>
          一度アカウントを削除すると、復活させることはできません
        </span>
        <span><i class="fas fa-exclamation-circle fa-red"></i>
          登録されている出退勤情報も削除されます
        </span>
      </div>
      <%= form_with model: @staff, url: employees_staff_path(@staff), method: :delete, remote: true do |f| %>
        <div class="mt-1e">
          <%= f.check_box :accept, :as => :boolean, checked: false%>
          <span id="accept">上記を確認のうえ、削除する</span>
        </div>
        <div class="text-center mt-1e">
          <%= f.submit "削除", class: "btn btn-danger" %>
        </div>
      <% end %>
    </div>
  </div>
</div>

