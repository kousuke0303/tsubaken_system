$(".staff-create-errors").html('<%= escape_javascript(render partial: "shared/error_messages", locals: { resource: @staff }) %>');
<% @staff.errors.messages.keys.each do |key_attribute| %>
  let addDiv = $('<div class="field-with-errors">');
  let addLabel = $('input[name="staff[<%= key_attribute.intern.to_s %>]"]').prev();
  $('input[name="staff[<%= key_attribute.intern.to_s %>]"]').wrap(addDiv);
  addLabel.wrap(addDiv);
<% end %>
  