（現在未着手・着工中の担当案件一覧）  
<% if @matters.present? %>
  <div class="table-responsive">
    <table class="table table-bordered text-nowrap table-column-type">
      <tr>
        <th>案件名</th>
        <th>案件内容</th>
        <th>現場住所</th>
        <th></th>
      </tr>
      <% @matters.each do |matter| %>
        <tr>
          <td><%= matter.title %></td>
          <td><%= matter.content %></td>
          <td><%= matter.prefecture_code %><%= matter.address_city %><%= matter.address_street %></td>
          <td>
            <%= link_to "担当者変更", change_member_employees_matter_path(matter, external_staff_id: @external_staff.id), remote: true, class: "btn btn-dark" %> 
          </td>
        </tr>
      <% end %>
    </table>
  </div>
<% else %>
  <p>現在未着手・着工中の担当案件はありません</p>
<% end %>

（営業担当案件一覧）
<% if @estimate_matters.present? %>
  <div class="table-responsive">
    <table class="table table-bordered text-nowrap table-column-type">
      <tr>
        <th>案件名</th>
        <th>案件内容</th>
        <th>現場住所</th>
        <th></th>
      </tr>
      <% @estimate_matters.each do |est_matter| %>
        <tr>
          <td><%= est_matter.title %></td>
          <td><%= est_matter.content %></td>
          <td><%= est_matter.prefecture_code %><%= est_matter.address_city %><%= est_matter.address_street %></td>
          <td>
            <%= link_to "担当者変更", change_member_employees_estimate_matter_path(est_matter, external_staff_id: @external_staff.id), remote: true, class: "btn btn-dark" %> 
          </td>
        </tr>
      <% end %>
    </table>
  </div>
<% else %>
  <p>未着工の営業担当案件はありません</p>
<% end %>