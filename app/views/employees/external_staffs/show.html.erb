<div class="col-lg-12 col-md-12">
  <h2><i class="fas fa-users"></i> <%= @external_staff.name %>詳細</h2>

  <div class="text-center tab-wrapper">
        
    <div>
      <table class="table table-bordered common-table" id="employees-external-staff-table">
        <tr>
          <th>所属</th>
          <td><%= @external_staff.supplier.name %></td>
        </tr>
        <tr>
          <th>名前</th>
          <td><%= @external_staff.name %></td>
        </tr>
        <tr>
          <th>フリガナ</th>
          <td><%= @external_staff.kana %></td>
        </tr>
        <tr>
          <th>ログインID</th>
          <td><%= @external_staff.login_id %></td>
        </tr>
        <tr>
          <th>電話番号</th>
          <td><%= @external_staff.phone %></td>
        </tr>
        <tr>
          <th>メールアドレス</th>
          <td><%= @external_staff.email %></td>
        </tr>
      </table>

      <div class="btn-area">
        <%= link_to "編集", edit_employees_supplier_external_staff_path(@supplier, @external_staff), remote: true, class: "btn btn-dark" %>
        <%= link_to "削除", employees_supplier_external_staff_path(@supplier, @external_staff), method: :delete, data: { confirm: "この外部Staffを削除してよろしいですか？" }, class: "btn btn-danger" %>
      </div>
    </div>
  </div>
</div>
