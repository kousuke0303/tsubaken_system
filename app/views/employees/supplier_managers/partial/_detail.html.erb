<div class="card">
  <div class="card-body">
    <%= form_with(model: @supplier_manager, url: employees_supplier_manager_path(@supplier_manager), remote: true) do |f| %>
      <div id="supplier-manager-update-errors"></div>
      <%= render 'supplier_managers/registrations/partial/form', f: f %>

      <% if current_admin || current_manager || self_supplier_manager %>
        <div class="btn-area">
          <%= f.submit "更新", class: "btn btn-dark" %>
        </div>
      <% end %>
    <% end %>
  </div>
</div>

<% if current_admin || current_manager || self_supplier_manager %>
  <div class="card mt-1e">
    <div class="card-body">
      <div id="pass-form">
      <%= render 'employees/supplier_managers/partial/pass_form' %>
      </div>
    </div>
  </div>
<% end %>