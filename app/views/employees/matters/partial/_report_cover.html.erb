<% if @report_cover.present? %>
  <div class="row report-cover-show">
    <div class="col-4">
      <%= image_tag url_for(@cover_img_1.image.variant(combine_options: { gravity: :center, resize: "300x300^",crop: "300x300+0+0" })) if @cover_img_1.present? %>
    </div>
    <div class="col-4">
      <%= image_tag url_for(@cover_img_2.image.variant(combine_options: { gravity: :center, resize: "300x300^",crop: "300x300+0+0" })) if @cover_img_2.present? %>
    </div>
    <div class="col-4">
      <%= image_tag url_for(@cover_img_3.image.variant(combine_options: { gravity: :center, resize: "300x300^",crop: "300x300+0+0" })) if @cover_img_3.present? %>
    </div>
    <div class="col-4">
      <%= image_tag url_for(@cover_img_4.image.variant(combine_options: { gravity: :center, resize: "300x300^",crop: "300x300+0+0" })) if @cover_img_4.present? %>
    </div>
  </div>
<% end %>
