<% target_est_matters.each do |month, est_matters| %>
  <tr>
    <td style="width: 3em;"><%= month %>月</td>
    <td>
      <% est_matters.each do |est_matter| %>
        <% if est_matter.sales_statuses.last.status == "appointment" %>
          <div class="husen" id="husen_<%= est_matter.id %>">
            <%= est_matter.title %>
            <div>
              <span style="font-size: 12px;">
                <%= est_matter.sales_statuses.last.scheduled_date.strftime("%m/%d") %>
                <%= member_name(est_matter.sales_statuses.last) %>
              </span>
            </div>
          </div>
          <script>
            var staff_color = "<%= staff_color(est_matter.sales_statuses.last.staff_id) %>";
            $('#husen_<%= est_matter.id %>').css({'border-left-style': 'solid',
                                                  'border-left-width': '10px',
                                                  'border-left-color': `${staff_color}`,
                                                  'background': `${staff_color}3b`});
          </script>
        <% end %>
      <% end %>
    </td>
    <td>
      <% est_matters.each do |est_matter| %>
        <% if est_matter.sales_statuses.last.status == "inspection" %>
          <div class="husen" id="husen_<%= est_matter.id %>">
            <%= est_matter.title %>
            <div>
              <span style="font-size: 12px;">
                <%= est_matter.sales_statuses.last.scheduled_date.strftime("%m/%d") %>
                <%= member_name(est_matter.sales_statuses.last) %>
              </span>
            </div>
          </div>
          <script>
            var staff_color = "<%= staff_color(est_matter.sales_statuses.last.staff_id) %>";
            $('#husen_<%= est_matter.id %>').css({'border-left-style': 'solid',
                                                  'border-left-width': '10px',
                                                  'border-left-color': `${staff_color}`,
                                                  'background': `${staff_color}3b`});
          </script>
        <% end %>
      <% end %>
    </td>
    <td>
      <% est_matters.each do |est_matter| %>
        <% if est_matter.sales_statuses.last.status == "calculation" %>
          <div class="husen" id="husen_<%= est_matter.id %>">
            <%= est_matter.title %>
            <div>
              <span style="font-size: 12px;">
                <%= est_matter.sales_statuses.last.scheduled_date.strftime("%m/%d") %>
                <%= member_name(est_matter.sales_statuses.last) %>
              </span>
            </div>
          </div>
          <script>
            var staff_color = "<%= staff_color(est_matter.sales_statuses.last.staff_id) %>";
            $('#husen_<%= est_matter.id %>').css({'border-left-style': 'solid',
                                                  'border-left-width': '10px',
                                                  'border-left-color': `${staff_color}`,
                                                  'background': `${staff_color}3b`});
          </script>
        <% end %>
      <% end %>
    </td>
    <td>
      <% est_matters.each do |est_matter| %>
        <% if est_matter.sales_statuses.last.status == "waiting_insurance" %>
          <div class="husen" id="husen_<%= est_matter.id %>">
            <%= est_matter.title %>
            <div>
              <span style="font-size: 12px;">
                <%= est_matter.sales_statuses.last.scheduled_date.strftime("%m/%d") %>
                <%= member_name(est_matter.sales_statuses.last) %>
              </span>
            </div>
          </div>
          <script>
            var staff_color = "<%= staff_color(est_matter.sales_statuses.last.staff_id) %>";
            $('#husen_<%= est_matter.id %>').css({'border-left-style': 'solid',
                                                  'border-left-width': '10px',
                                                  'border-left-color': `${staff_color}`,
                                                  'background': `${staff_color}3b`});
          </script>
        <% end %>
      <% end %>
    </td>
    <td>
      <% est_matters.each do |est_matter| %>
        <% if est_matter.sales_statuses.last.status == "immediately" %>
          <div class="husen" id="husen_<%= est_matter.id %>">
            <%= est_matter.title %>
            <div>
              <span style="font-size: 12px;">
                <%= est_matter.sales_statuses.last.scheduled_date.strftime("%m/%d") %>
                <%= member_name(est_matter.sales_statuses.last) %>
              </span>
            </div>
          </div>
          <script>
            var staff_color = "<%= staff_color(est_matter.sales_statuses.last.staff_id) %>";
            $('#husen_<%= est_matter.id %>').css({'border-left-style': 'solid',
                                                  'border-left-width': '10px',
                                                  'border-left-color': `${staff_color}`,
                                                  'background': `${staff_color}3b`});
          </script>
        <% end %>
      <% end %>
    </td>
    <td>
      <% est_matters.each do |est_matter| %>
        <% if est_matter.sales_statuses.last.status == "other" %>
          <div class="husen" id="husen_<%= est_matter.id %>">
            <%= est_matter.title %><
            <div>
              <span style="font-size: 12px;">
                <%= est_matter.sales_statuses.last.scheduled_date.strftime("%m/%d") %>
                <%= member_name(est_matter.sales_statuses.last) %>
              </span>
            </div>
          </div>
          <script>
            var staff_color = "<%= staff_color(est_matter.sales_statuses.last.staff_id) %>";
            $('#husen_<%= est_matter.id %>').css({'border-left-style': 'solid',
                                                  'border-left-width': '10px',
                                                  'border-left-color': `${staff_color}`,
                                                  'background': `${staff_color}3b`});
          </script>
        <% end %>
      <% end %>
    </td>
    <td>
      <% est_matters.each do |est_matter| %>
        <% if est_matter.sales_statuses.last.status == "chasing" %>
          <div class="husen" id="husen_<%= est_matter.id %>">
            <%= est_matter.title %>
            <div>
              <span style="font-size: 12px;">
                <%= est_matter.sales_statuses.last.scheduled_date.strftime("%m/%d") %>
                <%= member_name(est_matter.sales_statuses.last) %>
              </span>
            </div>
          </div>
          <script>
            var staff_color = "<%= staff_color(est_matter.sales_statuses.last.staff_id) %>";
            $('#husen_<%= est_matter.id %>').css({'border-left-style': 'solid',
                                                  'border-left-width': '10px',
                                                  'border-left-color': `${staff_color}`,
                                                  'background': `${staff_color}3b`});
          </script>
        <% end %>
      <% end %>
    </td>
    <td>
      <% est_matters.each do |est_matter| %>
        <% if est_matter.sales_statuses.last.status == "contract" %>
          <div class="husen" id="husen_<%= est_matter.id %>">
            <%= est_matter.title %>
            <div>
              <span style="font-size: 12px;">
                <%= est_matter.sales_statuses.last.scheduled_date.strftime("%m/%d") %>
                <%= member_name(est_matter.sales_statuses.last) %>
              </span>
            </div>
          </div>
          <script>
            var staff_color = "<%= staff_color(est_matter.sales_statuses.last.staff_id) %>";
            $('#husen_<%= est_matter.id %>').css({'border-left-style': 'solid',
                                                  'border-left-width': '10px',
                                                  'border-left-color': `${staff_color}`,
                                                  'background': `${staff_color}3b`});
          </script>
        <% end %>
      <% end %>
    </td>
    <td>
      <% est_matters.each do |est_matter| %>
        <% if est_matter.sales_statuses.last.status == "start_of_construction" %>
          <div class="husen" id="husen_<%= est_matter.id %>">
            <%= est_matter.title %>
            <div>
              <span style="font-size: 12px;">
                <%= est_matter.sales_statuses.last.scheduled_date.strftime("%m/%d") %>
                <%= member_name(est_matter.sales_statuses.last) %>
              </span>
            </div>
          </div>
          <script>
            var staff_color = "<%= staff_color(est_matter.sales_statuses.last.staff_id) %>";
            $('#husen_<%= est_matter.id %>').css({'border-left-style': 'solid',
                                                  'border-left-width': '10px',
                                                  'border-left-color': `${staff_color}`,
                                                  'background': `${staff_color}3b`});
          </script>
        <% end %>
      <% end %>
    </td>
    <td>
      <% est_matters.each do |est_matter| %>
        <% if est_matter.sales_statuses.last.status == "lost" %>
          <div class="husen" id="husen_<%= est_matter.id %>">
            <%= est_matter.title %>
            <div>
              <span style="font-size: 12px;">
                <%= est_matter.sales_statuses.last.scheduled_date.strftime("%m/%d") %>
                <%= member_name(est_matter.sales_statuses.last) %>
              </span>
            </div>
          </div>
          <script>
            var staff_color = "<%= staff_color(est_matter.sales_statuses.last.staff_id) %>";
            $('#husen_<%= est_matter.id %>').css({'border-left-style': 'solid',
                                                  'border-left-width': '10px',
                                                  'border-left-color': `${staff_color}`,
                                                  'background': `${staff_color}3b`});
          </script>
        <% end %>
      <% end %>
    </td>
    <td>
      <% est_matters.each do |est_matter| %>
        <% if est_matter.sales_statuses.last.status == "lost_by_other" %>
          <div class="husen" id="husen_<%= est_matter.id %>">
            <%= est_matter.title %>
            <div>
              <span style="font-size: 12px;">
                <%= est_matter.sales_statuses.last.scheduled_date.strftime("%m/%d") %>
                <%= member_name(est_matter.sales_statuses.last) %>
              </span>
            </div>
          </div>
          <script>
            var staff_color = "<%= staff_color(est_matter.sales_statuses.last.staff_id) %>";
            $('#husen_<%= est_matter.id %>').css({'border-left-style': 'solid',
                                                  'border-left-width': '10px',
                                                  'border-left-color': `${staff_color}`,
                                                  'background': `${staff_color}3b`});
          </script>
        <% end %>
      <% end %>
    </td>
    <td>
      <% est_matters.each do |est_matter| %>
        <% if est_matter.sales_statuses.last.status == "other" %>
          <div class="husen" id="husen_<%= est_matter.id %>">
            <%= est_matter.title %>
            <div>
              <span style="font-size: 12px;">
                <%= est_matter.sales_statuses.last.scheduled_date.strftime("%m/%d") %>
                <%= status_member_name(est_matter.sales_statuses.last) %>
              </span>
            </div>
          </div>
          <script>
            var staff_color = "<%= staff_color(est_matter.sales_statuses.last.staff_id) %>";
            $('#husen_<%= est_matter.id %>').css({'border-left-style': 'solid',
                                                  'border-left-width': '10px',
                                                  'border-left-color': `${staff_color}`,
                                                  'background': `${staff_color}3b`});
          </script>
        <% end %>
      <% end %>
    </td>
  </tr>
<% end %>