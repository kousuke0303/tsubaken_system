<div class="modal-dialog modal-lg">
  <div class="modal-content">
    <div class="modal-header"><button type="button" class="close" data-dismiss="modal">&times;</button></div>
    <div class="modal-body pa-2e">
      <div class="card mb-3">
        <h5 class="card-header">営業案件作成</h5>
        <div class="card-body">
          <%= form_with(model: @estimate_matter, url: employees_estimate_matters_path, method: :post) do |f| %>
            <div class="estimate-matter-create-errors"></div>
            <%= render "employees/estimate_matters/partial/form", f: f %>
            <div class="btn-area">
              <%= f.submit "作成", class: "btn btn-dark reset-errors" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

<%= javascript_include_tag "reset_errors.js" %>
