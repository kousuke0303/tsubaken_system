$("#estimate-category-edit").modal("hide");
$(".flash-wrapper").html('<div class="flash flash-notice time-limit">見積カテゴリを削除しました。</div>');
  $(".estimates-index").html('<%= escape_javascript(render "shared/estimate_matters/estimates_table") %>');
  $(function(){
    setTimeout("$('.time-limit').fadeOut('slow')", 3000) 
  })
  