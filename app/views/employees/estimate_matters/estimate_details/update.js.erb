$("#estimate-category-edit").modal("hide");
$(".flash-wrapper").html('<div class="flash flash-notice time-limit">工事・素材を追加しました。</div>');
$(".estimates-index").html('<%= escape_javascript(render partial: "shared/estimate_matters/estimates_index", locals: {estimates: @estimates}) %>');
$(function(){
  setTimeout("$('.time-limit').fadeOut('slow')", 3000) 
})
