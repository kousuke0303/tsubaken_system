$("#estimate-matter-show-modal").modal("hide");
<% if @type == "delete_category" %>
  toastr.warning('<%= @estimate_detail.category_name %>を削除しました');
<% elsif @type == "delete_object" %>
  toastr.warning('<%= @estimate_detail.object_name %>を削除しました');
<% end %>
$(".estimates-index").html('<%= escape_javascript(render "employees/estimate_matters/partial/estimates_index") %>');
$(function(){
  setTimeout("$('.time-limit').fadeOut('slow')", 3000) 
});
$('#estimate-matter-show-modal').empty();
