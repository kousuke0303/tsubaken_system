<div class="modal-dialog non-card-modal">
  <div class="modal-content">
    <div class="modal-header">
      見積書補足説明書項目編集
      <button type="button" class="close" data-dismiss="modal">&times;</button>
    </div>
    <div class="modal-body pa-2e">
      <%= form_with(model: @instruction, url: employees_settings_others_instruction_path, method: :patch, remote: true) do |f| %>
        <div class="instruction-update-errors"></div>
        <%= render "employees/settings/others/instructions/partial/form", f: f %>            
        <div class="btn-area">
          <%= f.submit "更新", class: "btn btn-dark reset-errors" %>
        </div>
      <% end %>
    </div>
  </div>
</div>