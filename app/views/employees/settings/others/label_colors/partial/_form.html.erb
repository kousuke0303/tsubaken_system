<div class="text-right">
  <%= link_to "カラーコード参照", "https://html-color-codes.info/japanese/", target: :blank %>
</div>

<%= f.label :name %>
<%= f.text_field :name, required: true, class: "form-control", maxlength: 10 %>

<%= f.label :color_code %>
<%= f.text_field :color_code, required: true, class: "form-control", maxlength: 7 %>

<%= f.label :note %>
<%= f.text_field :note, class: "form-control", maxlength: 30 %>

<div class="mt-1e">
  <% if @accept_type == 1 || @accept_type == 2 %>
    <div class="accept_text">
      <i class="fas fa-exclamation-circle"></i> 
      既作成の見積に使用されている色です。</br>
    </div>
  <% end %>
  <% if @accept_type == 1 || @accept_type == 3 %>
    <div class="accept_text">
      <i class="fas fa-exclamation-circle"></i> 
      既にSTAFFに設定されている色です。</br>
    </div>
  <% end %>
  <% unless @accept_type == 4 %>
    <div>
      これらも変更されますが、変更してよろしいですか？
    </div>
    <div class="custom-control custom-checkbox">
    <%= f.check_box :accept, { class: "custom-control-input", id: "custom-check-1" } %>
    <%= f.label :accept, "確認", class: "custom-control-label", for: "custom-check-1" %>
    </div>
  <% end %>
</div>