<div class="table-responsive">
  <table class="table table-bordered text-nowrap common-table">
    <thead>
      <tr>
        <th>名前</th>
        <th></th>
      </tr>
    </thead>
    <tbody id="sortable-categories">
      <% @categories.each do |category| %>
        <tr class="tr-sortable">
          <td><%= category.name %></td>
          <td>
            <%= link_to "編集", edit_employees_settings_category_path(category), remote: true, class: "btn btn-sm btn-dark" %>
            <%= link_to "削除", employees_settings_category_path(category), method: :delete, data: { confirm: "この工事名称を削除してよろしいですか？" }, class: "btn btn-sm btn-danger" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<%= javascript_include_tag "Sortable.min.js" %>
<script>
  $(function() {
    let el = document.getElementById("sortable-categories");
    if (el != null) {
      const sortable = Sortable.create(el, {
        animation: 150,
        delay: 100,
        onUpdate: function(evt) {
          $.ajax({
            type: "PATCH",
            url: "<%= sort_employees_settings_categories_path %>",
            cache: false,
            data: { from: evt.oldIndex, 
                    to: evt.newIndex, 
                    remote: true }
          });
        }
      });
    }
  });
</script>
