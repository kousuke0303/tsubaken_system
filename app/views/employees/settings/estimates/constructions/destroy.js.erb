$('#construction_table').html('<%= escape_javascript(render 'employees/settings/estimates/constructions/partial/index_table') %>');
<% if @responce == "success" %>
  toastr.info("<%= @construction.name %>を削除しました。");
<% else %>
  toastr.error("<%= @construction.name %>は見積もりで使用されているため、削除できません。");
<% end %>  